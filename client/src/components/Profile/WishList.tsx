import styles from './profile.module.scss';

const WishList = () => {
    return (
        <div className={styles.profileSection}>
            <h3>Wishlist</h3>
        </div>
    );
};

export default WishList;
